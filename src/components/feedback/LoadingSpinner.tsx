/**
 * LoadingSpinner — Réexporte depuis LogoLoader
 * Conservé pour la compatibilité avec les imports existants.
 */
export { FullPageLoader as PageLoader, InlineLoader as LoadingSpinner } from '@/components/ui/LogoLoader'
